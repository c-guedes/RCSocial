<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">
    <group android:checkableBehavior="single">
        <item android:id="@+id/btn_perfil"
            android:title="Perfil"
            android:icon="@drawable/usuario"
            android:state_checked="true" />
        <item android:id="@+id/btn_pesquisar"
            android:title="Pesquisar"
            android:icon="@drawable/pesquisar"
            android:visible="true"/>
        <item android:id="@+id/btn_auditar"
            android:title="Auditar"
            android:icon="@drawable/auditar" />
        <item android:id="@+id/btn_sair"
            android:title="Sair"
            android:icon="@drawable/sair"
            android:onClick=""/>
    </group>
</menu>